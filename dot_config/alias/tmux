# auto init (skip in SSH sessions)
if [ -x "$(command -v tmux)" ] && [ -z "${TMUX}" ] && [ -z "${SSH_CLIENT}" ] && [ -z "${SSH_TTY}" ]; then
    tmux attach || \
      exec tmux new-session -A -s main >/dev/null 2>&1
fi
