alias \
	watch-nvidia="watch -n 1 nvidia-smi" \
	which-gpu="glxinfo | grep 'OpenGL renderer'" \
	kill-origin="killall Origin.exe OriginWebHelperService.exe" \
    dmenu="rofi -dmenu" \
    xray="$HOME/.scripts/toggle_blur_xray.sh" # keyword no existe con provider lua
