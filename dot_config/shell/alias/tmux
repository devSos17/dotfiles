# auto init via sesh (skip in SSH sessions)
if [ -x "$(command -v sesh)" ] && [ -z "${TMUX}" ] && [ -z "${SSH_CLIENT}" ] && [ -z "${SSH_TTY}" ]; then
    sesh connect main 2>/dev/null || exec sesh connect main
fi
