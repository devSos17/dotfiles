# ── Local sessions (this machine only) ────────────────────────────────────────
# Managed by chezmoi as `create_`: se crea si no existe y NUNCA se sobreescribe.
# Edita libremente aquí; no hace falta commitear. Sesh lo lee en cada invocación.
