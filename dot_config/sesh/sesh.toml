import = [
    "~/.config/sesh/sessions-personal.toml",
    "~/.config/sesh/sessions-work.toml",
]

[default_session]
preview_command = "eza --all --git --icons --color=always -l {}"

# Dev projects: ticket dirs get kiro-cli, others get shell
# filepath.Match doesn't support ** so we cover multiple depths
[[wildcard]]
pattern = "~/dev/*"
startup_command = "ticket-session"

[[wildcard]]
pattern = "~/dev/*/*"
startup_command = "ticket-session"

[[wildcard]]
pattern = "~/dev/*/*/*"
startup_command = "ticket-session"

[[wildcard]]
pattern = "~/dev/*/*/*/*"
startup_command = "ticket-session"
