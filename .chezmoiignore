README.md
key.txt.age
oth
Makefile
bootstrap.sh

{{/* gui: flag de .chezmoi.toml.tmpl (bootstrap.sh lo fija por distro).
     Maquinas viejas sin el flag en su config -> se asume true (no rompe). */}}
{{- $gui := true }}
{{- if hasKey . "gui" }}{{ $gui = .gui }}{{ end }}
{{- $secrets := true }}
{{- if hasKey . "secrets" }}{{ $secrets = .secrets }}{{ end }}
{{ if not $secrets }}
# Templates que requieren bitwarden (bw) — sin secretos no renderizan
.config/todoist/config.json
.config/ulauncher/ext_preferences
# Archivos age-encriptados — sin key.txt no se pueden desencriptar
# (el --exclude encrypted del init no persiste en applies posteriores)
dev/.keys
{{ end }}
{{ if not $gui }}
# Stack de escritorio (solo con gui=true)
.config/hypr
.config/kanshi
.config/waybar
.config/wlogout
.config/rofi
.config/ulauncher
.config/swappy
.config/gtk-3.0
.config/qt6ct
.config/kitty
.config/mpv
.config/zathura
.config/pika-backup
.config/nut
.config/brave-flags.conf
{{ end }}

{{ if ne .chezmoi.os "darwin" }}
# Work-only (darwin)
.config/shell/hooks/pre/kiro
.config/shell/hooks/post/kiro
.config/shell/env/jira
{{ end }}
{{ if ne .chezmoi.os "linux" }}
dev
{{ end }}
